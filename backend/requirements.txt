# Exact pins. Regenerate deliberately, not casually — a hackathon does not want a
# transitive dependency changing behaviour between Day 1 and the demo slot.
# Verified installed and green (80 passed, 1 skipped) on Python 3.12.1.

# --- web / db (Salman's lane) ---
fastapi==0.141.1
uvicorn[standard]==0.52.4
sqlmodel==0.0.39
psycopg[binary]==3.3.4

# --- validation: the layer the whole AI lane is built on ---
pydantic==2.13.4
pydantic-settings==2.15.0

# --- model provider (Dev B's lane) ---
# Gemini is called over raw REST through httpx, NOT the google-genai SDK. Measured in S0:
# the SDK's auth path returns 403 PERMISSION_DENIED with our key, while the identical key
# works against the REST endpoint with an x-goog-api-key header. httpx is already pinned
# below, so the provider costs zero extra dependencies. See DECISIONS #39 and #45.
# Declared fallback. Pinned and installed on purpose so falling back is a one-class
# change rather than a dependency scramble on Day 3. Do not remove without #39 changing.
anthropic==1.1.0

# --- tests / http ---
# httpx is both the test client and the transport the LLMClient adapter can use directly,
# so a provider with no Python SDK needs no new dependency.
httpx==0.28.1
pytest==9.1.1
